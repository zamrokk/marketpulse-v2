manifest_version: subsquid.io/v0.1
name: marketpulse

build:

deploy:
  env:
    ETH_HTTP: ${{ secrets.ETH_HTTP }}
  addons:
    postgres:
  processor:
    cmd: ["sqd", "process:prod"]
  api:
    cmd: ["sqd", "serve:prod"]

